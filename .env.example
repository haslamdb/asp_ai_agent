# AWS Elastic Beanstalk Environment Variables Template
# Copy this file and fill in your actual values
# DO NOT commit .env with real credentials to git!

# =============================================================================
# REQUIRED: AI Model API Keys
# =============================================================================

# Anthropic Claude API Key (Claude 4.1 Opus, Claude 4.5 Sonnet/Haiku)
# Get from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Google Gemini API Key (Gemini 3 Pro, Gemini 2.5 Flash/Pro)
# Get from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# OpenAI API Key (GPT-5.1 Instant/Thinking, GPT-4o, GPT-4o Mini, GPT-4 Turbo)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# =============================================================================
# REQUIRED: Flask Environment
# =============================================================================

# Set to 'production' for AWS deployment
# Set to 'development' for local testing
FLASK_ENV=production

# =============================================================================
# OPTIONAL: Custom Database Paths
# =============================================================================

# Override default database locations if needed
# Default (local): asp_sessions.db
# Default (AWS): /var/app/current/data/asp_sessions.db
# ASP_DB_PATH=/custom/path/to/asp_sessions.db

# =============================================================================
# OPTIONAL: Server Configuration
# =============================================================================

# Port (automatically set by AWS Elastic Beanstalk)
# Default: 8080
# PORT=8080

# =============================================================================
# AWS Elastic Beanstalk Deployment
# =============================================================================

# To set these in AWS Elastic Beanstalk:
#
# Option 1: Using EB CLI (RECOMMENDED)
#   eb setenv ANTHROPIC_API_KEY=sk-ant-xxx GEMINI_API_KEY=xxx FLASK_ENV=production
#
# Option 2: Using AWS Console
#   1. Go to Elastic Beanstalk console
#   2. Select your environment
#   3. Configuration → Software → Environment properties
#   4. Add each variable
#
# Option 3: Using .ebextensions config file (NOT RECOMMENDED - secrets visible)
#   ⚠️ WARNING: This exposes secrets in your codebase. Use eb setenv instead!

# =============================================================================
# Local Development
# =============================================================================

# For local testing, create a .env file (copy this template):
#   cp .env.example .env
#   # Edit .env with your actual keys
#   # Load in terminal: export $(cat .env | xargs)
#   # Or use python-dotenv package

# Make sure .env is in .gitignore (it already is)

# =============================================================================
# OPTIONAL: reCAPTCHA Configuration (Spam Protection)
# =============================================================================

# Google reCAPTCHA v2 keys for signup form protection
# Get from: https://www.google.com/recaptcha/admin/create
# Select reCAPTCHA v2 "I'm not a robot" Checkbox
RECAPTCHA_SITE_KEY=your-recaptcha-site-key-here
RECAPTCHA_SECRET_KEY=your-recaptcha-secret-key-here
